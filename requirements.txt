# Runtime dependencies for the demo
semantica[shacl]>=0.6.6
spacy>=3.7

# The spaCy model used by local (no-LLM) entity extraction:
#   python -m spacy download en_core_web_sm

# Development / test dependencies
pytest>=8.0
